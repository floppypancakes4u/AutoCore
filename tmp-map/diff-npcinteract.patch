diff --git a/src/AutoCore.Game/Managers/NpcInteractHandler.cs b/src/AutoCore.Game/Managers/NpcInteractHandler.cs
index 2404a24..fca9140 100644
--- a/src/AutoCore.Game/Managers/NpcInteractHandler.cs
+++ b/src/AutoCore.Game/Managers/NpcInteractHandler.cs
@@ -1552,10 +1552,9 @@ public static class NpcInteractHandler
             if (!PatrolListsTarget(patrol, targetCoid))
                 continue;
 
-            // Prefer server-side range when the pad exists as a live entity, graphics template,
-            // or VisualWaypoint. Many mission GenericTargetCOIDs (e.g. Track This 10310ΓÇô10324)
-            // are client-only ghosts never present in continent .fam Templates ΓÇö client only
-            // emits AutoPatrol when already inside AutoCompleteDistance, so trust that gate.
+            // Client only emits AutoPatrol after its own AutoCompleteDistance check.
+            // Server map pad poses are often wrong/missing ΓÇö do not hard-reject on range
+            // (that freezes Live and Direct / LOA when the client is legitimately on the pad).
             if (TryGetWorldPosition(character.Map, targetCoid, out var targetPos))
             {
                 var radius = patrol.AutoCompleteDistance > 0f ? patrol.AutoCompleteDistance : 25f;
@@ -1563,26 +1562,15 @@ public static class NpcInteractHandler
                 if (distXZSq > radius * radius)
                 {
                     Logger.WriteLog(LogType.Debug,
-                        "AutoPatrol: target={0} out of range distXZ={1:F1} radius={2:F1} mission={3}",
+                        "AutoPatrol: target={0} server pos skew distXZ={1:F1} radius={2:F1} mission={3} ΓÇö trusting client",
                         targetCoid,
                         MathF.Sqrt(distXZSq),
                         radius,
                         quest.MissionId);
-                    continue;
                 }
             }
-            else
-            {
-                Logger.WriteLog(LogType.Debug,
-                    "AutoPatrol: target={0} listed, no map position on continent={1} mission={2} ΓÇö trusting client range gate",
-                    targetCoid,
-                    character.Map.ContinentId,
-                    quest.MissionId);
-            }
 
-            // Patrol + deliver on same objective (Final Exam class): reaching the pad waypoint
-            // must not finish the mission ΓÇö NPC deliver still required. Client sends AutoPatrol
-            // every tick while in volume; EnsureDeliverTurnInNpc is idempotent after first setup.
+            // Patrol + deliver on same objective (Final Exam class): pad alone must not finish.
             if (ObjectiveHasBlockingSiblingRequirements(objective, RequirementType.Patrol))
             {
                 foreach (var deliver in objective.Requirements.OfType<ObjectiveRequirementDeliver>())
@@ -1590,7 +1578,6 @@ public static class NpcInteractHandler
                     if (!deliver.NPCTargetCompletes || deliver.NPCTargetCBID <= 0)
                         continue;
 
-                    // Skip all work (and logging) once pad NPC is live and client was notified.
                     if (character.Map != null
                         && character.MapPresence.IsDeliverTurnInReady(deliver.NPCTargetCBID)
                         && character.Map.MapHasPresentEntityWithCbidForTests(
@@ -1613,6 +1600,62 @@ public static class NpcInteractHandler
             }
 
             LogPatrolIncomplete(patrol, quest, objective, targetCoid);
+
+            // Live and Direct class: one GenericTarget per objective sequence, Laps=1.
+            // Completing the pad must AdvanceOrComplete immediately so OnMissionStateChanged
+            // opens gates. LOA / multi-pad or multi-lap: track progress, advance only when done.
+            var listed = MissionPatrolProgress.CountListedTargets(patrol);
+            var multiLap = patrol.Laps > 1;
+            if (listed <= 1 && !multiLap)
+            {
+                AdvanceOrCompleteObjective(conn, character, quest, mission, objective, source: "AutoPatrol");
+                return;
+            }
+
+            var seq = quest.ActiveObjectiveSequence;
+            MissionKillProgress.EnsureProgressCapacity(quest, seq);
+            var hit = MissionPatrolProgress.TryApplyHit(
+                patrol,
+                quest.ObjectiveProgress[seq],
+                targetCoid);
+            if (!hit.Accepted)
+            {
+                Logger.WriteLog(LogType.Debug,
+                    "AutoPatrol: multi-pad target={0} ignored mission={1} seq={2} progress={3}",
+                    targetCoid,
+                    quest.MissionId,
+                    seq,
+                    quest.ObjectiveProgress[seq]);
+                return;
+            }
+
+            quest.ObjectiveProgress[seq] = hit.NewProgress;
+            quest.ObjectiveMax[seq] = Math.Max(hit.Needed, 1);
+
+            if (!hit.IsComplete)
+            {
+                Logger.WriteLog(LogType.Debug,
+                    "AutoPatrol: multi-pad partial mission={0} seq={1} objective={2} target={3} progress={4}/{5}",
+                    quest.MissionId,
+                    seq,
+                    objective.ObjectiveId,
+                    targetCoid,
+                    hit.DisplayProgress,
+                    hit.Needed);
+
+                // Absolute pad count for client GetTarget/Eval ΓÇö not a 0..1 ratio.
+                // Do NOT fire OnMissionStateChanged here (gates key off sequence advance).
+                var partialState = ObjectiveStateBuilder.BuildPatrolPadCount(
+                    objective,
+                    patrol,
+                    hit.DisplayProgress);
+                if (partialState != null)
+                    conn.SendGamePacket(partialState);
+                MissionPersistence.Instance.OnQuestChanged(character, quest);
+                PushJournalMissionList(conn, character);
+                return;
+            }
+
             AdvanceOrCompleteObjective(conn, character, quest, mission, objective, source: "AutoPatrol");
             return;
         }
@@ -1814,37 +1857,11 @@ public static class NpcInteractHandler
         MissionObjective objective,
         long targetCoid)
     {
+        // Multi-waypoint / Sequential / Laps are handled by MissionPatrolProgress.
         var listed = CountPatrolTargets(patrol);
         var context =
             $"mission={quest.MissionId} seq={quest.ActiveObjectiveSequence} objective={objective.ObjectiveId} target={targetCoid} listedTargets={listed}";
 
-        if (listed > 1)
-        {
-            IncompleteHandlerLog.Warn(
-                "AutoPatrol",
-                context,
-                "Multi-waypoint patrol: any listed target currently completes the entire objective (no per-point progress)",
-                "Track visited GenericTargets (Sequential order if Sequential=true); only AdvanceOrComplete when all points (or current lap) are done; update ObjectiveState slots/bitmask per FirstStateSlot.");
-        }
-
-        if (patrol.Laps > 1)
-        {
-            IncompleteHandlerLog.Warn(
-                "AutoPatrol",
-                context,
-                $"Laps={patrol.Laps} ignored ΓÇö single pass completes the objective",
-                "Maintain lap counter on CharacterQuest progress; require Laps full circuits before complete.");
-        }
-
-        if (patrol.Sequential && listed > 1)
-        {
-            IncompleteHandlerLog.Warn(
-                "AutoPatrol",
-                context,
-                "Sequential=true ignored ΓÇö targets may be hit in any order",
-                "Enforce GenericTargets order; reject or ignore out-of-order AutoPatrol until previous COID cleared.");
-        }
-
         if (patrol.AutoFail)
         {
             IncompleteHandlerLog.Warn(
