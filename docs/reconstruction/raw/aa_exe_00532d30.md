# Raw decompiler capture: CVOGCharacter_LevelUp

| Field | Value |
|-------|-------|
| Stable ID | `aa_exe_00532d30` |
| Binary | autoassault.exe |
| Address | `0x00532d30` |
| Original symbol | `CVOGCharacter_LevelUp` |
| System | progression |
| Decompiler | Ghidra MCP batch_decompile |
| Timestamp | 2026-07-15T20:00:00Z |
| Capture version | v1 |

## Notes

- Called from `CVOGReaction_AddExperience` level-up loop with `bNotifyUi=true`.
- Looks up `tExperienceLevel` map for **new level** (`current+1`).
- On hit: dirty flag `object+0x180 |= 0x80`; `nLevel++`; add skill/attrib from row `+0x14` (packed shorts); research from row `+0x18` → char `+0x580`.
- Skill HB refresh (character + vehicle skill lists); `Vehicle_RecalcCombatPools` if vehicle present.
- Local player bit → `CVOGCharacter_SearchAutoMissions`.
- Optional LogicUI type `0x2D` when `bNotifyUi`.

## Raw pseudocode (summary body)

Full plate-commented decompile captured 2026-07-15 via Ghidra MCP. Key control flow:

```c
void __thiscall CVOGCharacter_LevelUp(void *this, bool bNotifyUi)
{
  int newLevel = *(int*)((char*)this + 0x6c8) + 1;
  if (!g_nExperienceLevelTableReady)
    Experience_EnsureLevelTableLoaded_INFERRED();

  // map lower_bound by newLevel → ExperienceLevelMapEntry_INFERRED
  // if end: return

  // row: +0x10 intExperience, +0x14 skill|attrib packed, +0x18 research
  *(byte*)(base+0x180) |= 0x80;
  *(int*)((char*)this + 0x6c8) += 1;
  *(short*)((char*)this + 0x6cc) += (short)skillAttribPacked;
  *(short*)((char*)this + 0x6ce) += (short)(skillAttribPacked >> 16);
  // skill HB refresh loops (character then vehicle+0x250)
  *(short*)((char*)this + 0x580) += (short)researchGrant;
  // FUN_004c2ef0/ee0/f00/f10 attribute refresh
  if (vehicle) Vehicle_RecalcCombatPools(...);
  // local player: CVOGCharacter_SearchAutoMissions
  if (bNotifyUi) Client_SendLogicUiPacket(/* type 0x2D, level, ... */);
}
```

See session decompile for full skill-vector / exception-frame detail.
