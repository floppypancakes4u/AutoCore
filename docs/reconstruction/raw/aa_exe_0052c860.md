# Raw decompiler capture: Experience_GetCumulativeThreshold

| Field | Value |
|-------|-------|
| Stable ID | `aa_exe_0052c860` |
| Binary | autoassault.exe |
| Address | `0x0052c860` |
| Original symbol | `Experience_GetCumulativeThreshold` |
| System | progression |
| Decompiler | Ghidra MCP batch_decompile |
| Timestamp | 2026-07-15T20:00:00Z |
| Capture version | v1 |

## Notes

- Returns `tExperienceLevel.intExperience` for given level from lazy-loaded std::map.
- Missing key → `0x7fffffff` (level-loop abort sentinel in `AddExperience`).
- Row XP field at map node `+0x10`.

## Raw pseudocode

```c

/* Experience_GetCumulativeThreshold — tExperienceLevel.intExperience
   
   Parameters: wLevel (ushort)
   Returns: cumulative XP, or 0x7FFFFFFF if missing (max sentinel)
   Lazy-inits via Experience_EnsureLevelTableLoaded_INFERRED
   Map entry type: ExperienceLevelMapEntry_INFERRED */

uint __cdecl Experience_GetCumulativeThreshold(ushort wLevel)

{
  void *pvVar1;
  int *extraout_EAX;
  uint uVar2;
  void *abScratch;
  
  _wLevel = (uint)wLevel;
  if (g_nExperienceLevelTableReady == 0) {
    Experience_EnsureLevelTableLoaded_INFERRED();
  }
  StdMap_LowerBoundByIntKey_INFERRED(&g_pExperienceLevelMap_INFERRED,&abScratch,(int *)&wLevel);
  pvVar1 = (void *)*extraout_EAX;
  if (g_nExperienceLevelTableReady == 0) {
    Experience_EnsureLevelTableLoaded_INFERRED();
  }
  uVar2 = 0x7fffffff;
  if (pvVar1 != g_pExperienceLevelMapEnd) {
    uVar2 = *(uint *)((int)pvVar1 + 0x10);
  }
  return uVar2;
}
```
